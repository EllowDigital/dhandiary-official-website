# Ensure project installs include devDependencies (Vite is required for build/dev)
# This overrides any global npm config like `omit=dev`.
# Do NOT omit optional deps (Rollup uses optional native binaries on Windows).
omit=peer
