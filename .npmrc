# Ensure devDependencies (vite/prettier/eslint/tsc) are installed even if the user has a global config like `omit=dev`.
# This avoids using the deprecated `production` npm config (which prints warnings).
omit=
